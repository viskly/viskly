# CLA signatures

Written by the CLA workflow (.github/workflows/cla.yml) when a contributor signs CLA.md.
Kept off main because main accepts changes only through pull requests, and the
workflow commits here directly.
